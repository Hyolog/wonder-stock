<UserControl x:Class="WonderStock.UserControls.SimpleStockTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WonderStock.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" Margin="5">
        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="20"/>
        <TextBlock Text="{Binding Code, StringFormat={}({0})}" VerticalAlignment="Center" FontSize="20"/>
    </StackPanel>
</UserControl>
